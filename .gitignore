# Claude Code / OMC 运行态
.claude/
.omc/

# 编译产物
bin/
dist/

# 开发用自签证书（勿提交，用 make certs 现生成）
certs/

# osqueryd 二进制（~110MB/平台，勿提交，用 deploy/osquery/fetch.sh 现拉）
deploy/osquery/bin/

# embed 用 osqueryd（构建 -tags embedosq 时 fetch+gzip 生成，勿提交）
internal/agent/osq/assets/*.gz

# 本地配置
*.local.yaml
*.local.yml
*.local.json
*.local.env
.env.local

# Go 工具链
vendor/

# macOS
.DS_Store
